Start a local preview server that matches how Atomic serves this site. Run:

```
python3 -m http.server 8000
```

in the repo root directory. This is the same server Atomic uses in production. Tell the user the site is available at http://localhost:8000 and remind them to stop it with Ctrl+C when done.
